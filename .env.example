# .env.example - Plantilla para variables de entorno de producción
# Copia este archivo como .env y ajusta los valores antes de desplegar

# === PostgreSQL (Base de datos principal) ===
POSTGRES_USER=admin
POSTGRES_PASSWORD=supersecreto
POSTGRES_DB=criminal_cross
# Para que Quarkus se conecte a Postgres dentro de la red de Docker:
DB_URL=jdbc:postgresql://postgres:5432/criminal_cross
DB_USERNAME=admin
DB_PASSWORD=supersecreto

# === MongoDB (Activity Schedulers, etc) ===
MONGO_INITDB_ROOT_USERNAME=mongoadmin
MONGO_INITDB_ROOT_PASSWORD=mongosecreto
MONGO_INITDB_DATABASE=criminal_cross_mongo
# Para que Quarkus se conecte a Mongo dentro de la red de Docker:
MONGO_URL=mongodb://mongoadmin:mongosecreto@mongo:27017/criminal_cross_mongo?authSource=admin
MONGO_DB=criminal_cross_mongo

# === Firebase (Autenticación) ===
# Ruta al archivo JSON con las credenciales (Debe estar montado en el contenedor)
FIREBASE_CREDENTIALS_PATH=/app/secrets/firebase-adminsdk.json

# === Seguridad y JWT ===
# Rutas a las llaves PEM generadas para firmar y validar tokens (Debe estar montado en el contenedor)
# Ejemplo para generar llaves:
# openssl rsa -in rsaPrivateKey.pem -pubout -outform PEM -out rsaPublicKey.pem
JWT_PRIVATE_KEY_LOCATION=/app/secrets/rsaPrivateKey.pem
JWT_PUBLIC_KEY_LOCATION=/app/secrets/rsaPublicKey.pem
