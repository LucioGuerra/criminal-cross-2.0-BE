---
phase: 02-schedule-crud-completion
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/test/java/org/athlium/gym/application/usecase/DeleteActivityScheduleUseCaseTest.java
  - src/test/java/org/athlium/gym/application/usecase/UpdateActivityScheduleUseCaseTest.java
  - src/test/java/org/athlium/gym/presentation/controller/ActivityScheduleResourceUnitTest.java
autonomous: true
requirements: [SCHED-03, SCHED-04]

must_haves:
  truths:
    - "DeleteActivityScheduleUseCase is tested: soft-deletes schedule (active=false), throws 404 for non-existent ID, throws 400 for null ID"
    - "UpdateActivityScheduleUseCase is tested: updates mutable fields, preserves identity fields, throws 404 for non-existent ID, throws 400 for null inputs"
    - "ActivityScheduleResource DELETE endpoint is tested: returns 200 on success, 404 for missing schedule, 400 for bad input"
    - "ActivityScheduleResource PUT endpoint is tested: returns 200 on success, 404 for missing schedule, 400 for bad input"
    - "All tests use in-memory test doubles (no Mockito) following established project conventions"
  artifacts:
    - path: "src/test/java/org/athlium/gym/application/usecase/DeleteActivityScheduleUseCaseTest.java"
      provides: "Unit tests for soft-delete use case"
      min_lines: 60
    - path: "src/test/java/org/athlium/gym/application/usecase/UpdateActivityScheduleUseCaseTest.java"
      provides: "Unit tests for update use case"
      min_lines: 80
    - path: "src/test/java/org/athlium/gym/presentation/controller/ActivityScheduleResourceUnitTest.java"
      provides: "Unit tests for DELETE and PUT controller endpoints"
      min_lines: 100
  key_links:
    - from: "DeleteActivityScheduleUseCaseTest.java"
      to: "DeleteActivityScheduleUseCase"
      via: "Direct instantiation + InMemoryActivityScheduleRepository"
      pattern: "new DeleteActivityScheduleUseCase"
    - from: "UpdateActivityScheduleUseCaseTest.java"
      to: "UpdateActivityScheduleUseCase"
      via: "Direct instantiation + InMemoryActivityScheduleRepository"
      pattern: "new UpdateActivityScheduleUseCase"
    - from: "ActivityScheduleResourceUnitTest.java"
      to: "ActivityScheduleResource"
      via: "Direct instantiation + stub use cases injected via field assignment"
      pattern: "resource\\.deleteActivityScheduleUseCase ="
---

<objective>
Add unit tests for the update and soft-delete operations created in plan 02-01 — covering both use cases and the controller endpoints.

Purpose: Fulfill SCHED-03 and SCHED-04 requirements, ensuring the update and delete logic is verified with automated tests before shipping.
Output: Three test files covering use case logic and controller HTTP response mapping.
</objective>

<execution_context>
@/home/nacho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/nacho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schedule-crud-completion/02-01-SUMMARY.md

Pattern references (existing test conventions — follow these exactly):
@src/test/java/org/athlium/gym/application/usecase/CreateActivityScheduleUseCaseTest.java
@src/test/java/org/athlium/gym/presentation/controller/SessionResourceUnitTest.java

Source files being tested (created in plan 02-01):
@src/main/java/org/athlium/gym/application/usecase/DeleteActivityScheduleUseCase.java
@src/main/java/org/athlium/gym/application/usecase/UpdateActivityScheduleUseCase.java
@src/main/java/org/athlium/gym/presentation/controller/ActivityScheduleResource.java
@src/main/java/org/athlium/gym/presentation/mapper/ActivityScheduleDtoMapper.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for DeleteActivityScheduleUseCase and UpdateActivityScheduleUseCase</name>
  <files>
    src/test/java/org/athlium/gym/application/usecase/DeleteActivityScheduleUseCaseTest.java
    src/test/java/org/athlium/gym/application/usecase/UpdateActivityScheduleUseCaseTest.java
  </files>
  <action>
    **Follow the exact pattern from `CreateActivityScheduleUseCaseTest.java`:** Direct instantiation with `new`, inject fields directly (`useCase.activityScheduleRepository = repository`), use inner static `InMemoryActivityScheduleRepository` class, NO Mockito.

    **The InMemoryActivityScheduleRepository must implement the UPDATED `ActivityScheduleRepository` interface** which now includes `findById(Long id)` (added in plan 02-01). Extend the existing inner class from `CreateActivityScheduleUseCaseTest` with:
    - A `Map<Long, ActivitySchedule>` or `List<ActivitySchedule>` for storage
    - `findById(Long id)` that returns the schedule matching `schedule.getId() == id`, or null if not found
    - `save(ActivitySchedule)` that stores the schedule in the collection and returns it
    - `findAllActive()` and `findByHeadquartersId()` can throw `UnsupportedOperationException("Not used in this test")`

    **DeleteActivityScheduleUseCaseTest** — test cases:
    1. `shouldSoftDeleteExistingSchedule` — pre-store a schedule with `active=true`, call `execute(id)`, assert returned schedule has `active=false`, assert stored schedule has `active=false`
    2. `shouldThrowEntityNotFoundWhenScheduleDoesNotExist` — call `execute(999L)` with empty repository, assert `EntityNotFoundException` is thrown
    3. `shouldThrowBadRequestWhenIdIsNull` — call `execute(null)`, assert `BadRequestException` with message "Schedule ID is required"

    **UpdateActivityScheduleUseCaseTest** — test cases:
    1. `shouldUpdateMutableFieldsOnExistingSchedule` — pre-store a schedule, create updatedData with new `startTime`, `durationMinutes`, `dayOfWeek`, call `execute(id, updatedData)`, assert returned schedule has the new field values
    2. `shouldPreserveIdentityFieldsOnUpdate` — pre-store a schedule with specific `organizationId`, `headquartersId`, `activityId`, call `execute(id, updatedData)` where updatedData has DIFFERENT identity values, assert returned schedule still has the ORIGINAL identity values (these fields must NOT be updated)
    3. `shouldThrowEntityNotFoundWhenScheduleDoesNotExist` — call `execute(999L, someData)`, assert `EntityNotFoundException`
    4. `shouldThrowBadRequestWhenIdIsNull` — call `execute(null, someData)`, assert `BadRequestException`
    5. `shouldThrowBadRequestWhenUpdatedDataIsNull` — call `execute(1L, null)`, assert `BadRequestException`

    Use `@BeforeEach` for setup. Use `baseSchedule()` helper method for creating test schedules (same pattern as CreateActivityScheduleUseCaseTest). Import from `org.athlium.shared.exception.BadRequestException` and `org.athlium.shared.exception.EntityNotFoundException`.
  </action>
  <verify>
    Run `mvn test -pl backend -Dtest="DeleteActivityScheduleUseCaseTest,UpdateActivityScheduleUseCaseTest" 2>&1 | tail -20` — all tests must pass. Expect 3 tests in Delete, 5 in Update = 8 total passing.
  </verify>
  <done>
    - DeleteActivityScheduleUseCaseTest has 3 passing tests: soft-delete success, not-found exception, null-id exception
    - UpdateActivityScheduleUseCaseTest has 5 passing tests: update success, identity preservation, not-found, null-id, null-data
    - Both test files use InMemoryActivityScheduleRepository (no Mockito)
    - All assertions use JUnit 5 assertEquals/assertThrows
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for ActivityScheduleResource DELETE and PUT endpoints</name>
  <files>
    src/test/java/org/athlium/gym/presentation/controller/ActivityScheduleResourceUnitTest.java
  </files>
  <action>
    **Follow the exact pattern from `SessionResourceUnitTest.java`:** Direct `new ActivityScheduleResource()` instantiation, inject stub use cases and mapper via field assignment, stub use cases extend the concrete classes and override `execute()`, stub mapper implements/extends `ActivityScheduleDtoMapper`.

    **Create stub inner classes:**

    1. `StubDeleteActivityScheduleUseCase extends DeleteActivityScheduleUseCase` — fields: `ActivitySchedule response`, `boolean throwNotFound`, `boolean throwBadRequest`. Override `execute(Long id)`: if `throwNotFound` → throw `EntityNotFoundException("Activity schedule not found")`, if `throwBadRequest` → throw `BadRequestException(...)`, otherwise return `response`.

    2. `StubUpdateActivityScheduleUseCase extends UpdateActivityScheduleUseCase` — fields: `ActivitySchedule response`, `boolean throwNotFound`, `boolean throwBadRequest`. Override `execute(Long id, ActivitySchedule data)`: same pattern.

    3. `StubActivityScheduleDtoMapper extends ActivityScheduleDtoMapper` — override `toResponse(schedule)` to return a new `ActivityScheduleResponse()` (minimal), override `toDomain(request)` to return a new `ActivitySchedule()`, override `toResponseList(list)` to map each. Since `ActivityScheduleDtoMapper` is a concrete class (not interface), extend it and override the public methods.

    **Setup in `@BeforeEach`:**
    ```java
    resource = new ActivityScheduleResource();
    stubDelete = new StubDeleteActivityScheduleUseCase();
    stubUpdate = new StubUpdateActivityScheduleUseCase();
    resource.deleteActivityScheduleUseCase = stubDelete;
    resource.updateActivityScheduleUseCase = stubUpdate;
    resource.mapper = new StubActivityScheduleDtoMapper();
    // Also inject existing use cases to avoid NPE (create, get, generate stubs that are not tested here)
    resource.createActivityScheduleUseCase = new StubCreateActivityScheduleUseCase();
    resource.getActivitySchedulesUseCase = new StubGetActivitySchedulesUseCase();
    resource.generateNextWeekSessionsUseCase = new StubGenerateNextWeekSessionsUseCase();
    ```

    IMPORTANT: The existing use cases (`CreateActivityScheduleUseCase`, `GetActivitySchedulesUseCase`, `GenerateNextWeekSessionsUseCase`) are `@Inject`-ed fields on the resource. You must also create minimal stubs for them (just extend and return null/empty) to avoid NPE during resource construction, even though we don't test those endpoints here.

    **Test cases for DELETE endpoint:**
    1. `shouldReturn200WhenDeleteSucceeds` — set `stubDelete.response` to an `ActivitySchedule` with `active=false`, call `resource.delete(1L)`, assert status 200, assert `ApiResponse.isSuccess() == true`
    2. `shouldReturn404WhenDeleteScheduleNotFound` — set `stubDelete.throwNotFound = true`, call `resource.delete(99L)`, assert status 404, assert `ApiResponse.isSuccess() == false`
    3. `shouldReturn400WhenDeleteWithBadInput` — set `stubDelete.throwBadRequest = true`, call `resource.delete(null)`, assert status 400

    **Test cases for PUT endpoint:**
    4. `shouldReturn200WhenUpdateSucceeds` — set `stubUpdate.response` to an updated `ActivitySchedule`, call `resource.update(1L, new ActivityScheduleRequest())`, assert status 200, assert `ApiResponse.isSuccess() == true`
    5. `shouldReturn404WhenUpdateScheduleNotFound` — set `stubUpdate.throwNotFound = true`, call `resource.update(99L, new ActivityScheduleRequest())`, assert status 404
    6. `shouldReturn400WhenUpdateWithBadInput` — set `stubUpdate.throwBadRequest = true`, call `resource.update(1L, new ActivityScheduleRequest())`, assert status 400

    Cast `response.getEntity()` to `ApiResponse<?>` for assertion (same pattern as `SessionResourceUnitTest`). Import from `jakarta.ws.rs.core.Response`, `org.athlium.shared.dto.ApiResponse`.
  </action>
  <verify>
    Run `mvn test -pl backend -Dtest="ActivityScheduleResourceUnitTest" 2>&1 | tail -20` — all 6 tests must pass.
  </verify>
  <done>
    - ActivityScheduleResourceUnitTest has 6 passing tests: DELETE success/404/400, PUT success/404/400
    - Tests use stub inner classes extending concrete use case classes (no Mockito)
    - Response status codes and ApiResponse success/error flags verified
    - Mapper is stubbed to avoid testing mapper logic in controller tests
  </done>
</task>

</tasks>

<verification>
- `mvn test -pl backend -Dtest="DeleteActivityScheduleUseCaseTest,UpdateActivityScheduleUseCaseTest,ActivityScheduleResourceUnitTest"` — all 14 tests pass
- `mvn test -pl backend` — full test suite passes (no regressions)
- All 3 test files exist and follow project conventions (no Mockito, inner static stubs, JUnit 5)
</verification>

<success_criteria>
- 8 use case tests pass (3 delete + 5 update) covering happy path, not-found, and bad-request scenarios
- 6 controller tests pass (3 DELETE + 3 PUT) verifying HTTP status codes and ApiResponse wrapping
- All tests use in-memory/stub test doubles — zero Mockito usage
- Full `mvn test` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-schedule-crud-completion/02-02-SUMMARY.md`
</output>
