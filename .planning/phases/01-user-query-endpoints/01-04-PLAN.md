---
phase: 01-user-query-endpoints
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/org/athlium/users/infrastructure/repository/UserQueryRepositoryImpl.java
autonomous: true
requirements: [USER-01, USER-02, USER-03]
gap_closure: true

must_haves:
  truths:
    - "GET /api/users?headquartersId={id}&sort=name:asc returns results sorted by name without SQL error"
    - "GET /api/users?organizationId={id}&sort=lastName:desc returns results sorted by last_name without SQL error"
    - "GET /api/users?sort=email:asc returns results sorted by email without SQL error"
    - "Default sort (no sort param) still works correctly (name ASC)"
    - "All 40 Phase 1 unit tests pass after clean compile"
  artifacts:
    - path: "src/main/java/org/athlium/users/infrastructure/repository/UserQueryRepositoryImpl.java"
      provides: "Fixed SORT_FIELD_MAP without table alias prefix"
      contains: '"name", "name"'
  key_links:
    - from: "UserQueryRepositoryImpl.SORT_FIELD_MAP"
      to: "buildSortClause()"
      via: "map lookup for ORDER BY column"
      pattern: 'SORT_FIELD_MAP.get\(field\)'
---

<objective>
Fix SQL sort clause bug that causes runtime errors on any sorted user query.

Purpose: The SORT_FIELD_MAP in UserQueryRepositoryImpl uses table alias prefix "u." (e.g., "u.name") in its values, but the ORDER BY clause is appended outside the CTE where alias "u" does not exist. This causes a SQL error on every request that includes a sort parameter for all three paginated endpoints (by HQ, by org, all users).

Output: Fixed SORT_FIELD_MAP values without alias prefix, all sorted queries execute correctly.
</objective>

<execution_context>
@/home/nacho/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/nacho/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-user-query-endpoints/01-02-SUMMARY.md
@src/main/java/org/athlium/users/infrastructure/repository/UserQueryRepositoryImpl.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SORT_FIELD_MAP alias prefix and verify all tests pass</name>
  <files>src/main/java/org/athlium/users/infrastructure/repository/UserQueryRepositoryImpl.java</files>
  <action>
In UserQueryRepositoryImpl.java at lines 28-32, change the SORT_FIELD_MAP values to remove the "u." table alias prefix:

Change:
```java
private static final Map<String, String> SORT_FIELD_MAP = Map.of(
        "name", "u.name",
        "lastName", "u.last_name",
        "email", "u.email"
);
```

To:
```java
private static final Map<String, String> SORT_FIELD_MAP = Map.of(
        "name", "name",
        "lastName", "last_name",
        "email", "email"
);
```

Why: The ORDER BY clause is appended at lines 136, 266, and 454 OUTSIDE the CTE, where the result columns are just "name", "last_name", "email" (from `best_status WHERE rn = 1`). The "u." alias only exists inside the CTE's `user_packages` subquery. Using "u.name" outside the CTE causes a PostgreSQL error like "missing FROM-clause entry for table u".

The default sort in buildSortClause() (line 592) already correctly returns "name ASC" without prefix — only the map values are wrong.

After the fix, also run `mvn clean test` (clean is needed to clear stale Lombok-compiled classes that were causing 4 false test failures in GetUserByIdUseCaseTest — the test code itself is correct).
  </action>
  <verify>
1. `mvn clean compile -pl . -q` succeeds
2. `mvn test -Dtest="GetUsersByHqUseCaseTest,GetUsersByOrgUseCaseTest,GetAllUsersUseCaseTest,GetUserByIdUseCaseTest,UserQueryResourceUnitTest" -pl . -q` — all 40 tests pass with 0 failures
3. Grep the file to confirm no "u." prefix remains in SORT_FIELD_MAP: `grep -n 'u\.' UserQueryRepositoryImpl.java` should show only SQL CTE aliases inside query strings (lines inside triple-quoted SQL), NOT in the SORT_FIELD_MAP (lines 28-32)
  </verify>
  <done>
SORT_FIELD_MAP values are "name", "last_name", "email" (no alias prefix). All 40 unit tests pass. The three paginated endpoints (findUsersByHeadquarters, findUsersByOrganization, findAllUsers) will generate correct ORDER BY clauses like "ORDER BY name ASC" instead of broken "ORDER BY u.name ASC".
  </done>
</task>

</tasks>

<verification>
1. SORT_FIELD_MAP contains no "u." prefix in values
2. buildSortClause() still falls back to "name ASC" for null/invalid sort
3. All 40 Phase 1 unit tests pass (mvn clean test)
4. Compilation succeeds with no errors
</verification>

<success_criteria>
- SORT_FIELD_MAP maps: "name" → "name", "lastName" → "last_name", "email" → "email"
- All sort-related ORDER BY clauses outside CTEs use unaliased column names
- 40/40 unit tests pass
- The 3 blocker UAT issues (Tests 1, 2, 3, 5) are resolved
</success_criteria>

<output>
After completion, create `.planning/phases/01-user-query-endpoints/01-04-SUMMARY.md`
</output>
